{
  "name": "react-conta-tempo",
  "version": "1.0.0",
  "description": "My first React project. Counts time spent since the page was open",
  "main": "conta-tempo.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Igor Couto",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.11.1",
    "install": "^0.10.4",
    "npm": "^5.6.0"
  }
}
